{
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "env": {
                "SITE_AGENT_CONFIG_FILE": "~/git/camerapassword/site-agent/site-agent_new.cfg"
            }
        }
    ]
}